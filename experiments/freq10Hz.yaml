# Experiment Configuration: MTU Filter 10 Hz (New Test)
experiment_name: freq10Hz
description: Test experiment with 10 Hz MTU length filter

# Filter frequencies
filter_freq: 15                    # Kinematics filter frequency (Hz)
coord_filter_freq: 10             # Coordinate filter frequency (Hz)
mtu_length_filter_freq: 10        # MTU length filter frequency (Hz) - NEW TEST

# Session parameters
subject_num: 2
date: March_2
session: '7'
type: sprint

# Enable diagnostics
enable_mtu_filter_diagnostics: false

# Paths
paths:
  base: 'G:/Shared drives/Stanford Football'
  output_base: 'Outputs'          # Will create Outputs/freq10Hz/

# Scripts to run in pipeline (optional)
scripts:
  - example_cleaned
  - CalcStrideMaxLastThreeStrides
  - SeparateSteps
  - CalcStepVelReedMethodWithFlags
